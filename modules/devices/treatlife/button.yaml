---
platform: gpio
pin:
  inverted: true
  number: ${gpio_pin}
name: '${friendly_name} Button ${number}'
internal: true
filters:
  - delayed_on_off: 50ms
on_press:
  ${output_${number}_type}.toggle: '${output_${number}_type}${number}'
